#include <stdio.h>

char kata[] = "Tolol";

int urutan;

int hitungPanjang(const char *str) {
	int panjang = 0;
	while (str[panjang] != '\0') {
		panjang++;
	}
	return panjang;
}

int length = sizeof(kata) / sizeof(kata[0]) - 1;

void sorting(const int){
    int lowest, i;
    i = 0;
    lowest = int[0];
    for (i = 0; i < length; i++)
    {
        if 
    }
    
}

int main(){
    printf("Masukkan angka yang diinginkan: ");
    scanf("%d %d %d %d %d", &urutan);
    printf("Data yang dimasukkan adalah %d", urutan);

}